# Full pipeline. Install with:
#     python -m pip install -r requirements.txt
# This is a package list, not a script -- do not run it with python.

# --- agent core ----------------------------------------------------------
langchain>=1.0
langgraph>=1.0
python-dotenv>=1.0

# --- model providers (install the one you use; all listed for convenience) -
langchain-google-genai>=2.0        # FREE tier: Gemini Flash / Flash-Lite
langchain-groq>=0.3                # FREE tier: Llama on Groq
langchain-openai>=1.0              # paid
# langchain-ollama>=0.3            # fully local, uncomment if using Ollama

# --- observability ---------------------------------------------------------
# langfuse>=4.0                     # optional, real tracing; unset keys -> local traces.jsonl

# --- search --------------------------------------------------------------
ddgs>=9.0                          # FREE, keyless DuckDuckGo
langchain-tavily>=0.2.17           # paid, better quality

# --- analysis ------------------------------------------------------------
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0
tqdm>=4.66

# --- notebook ------------------------------------------------------------
jupyterlab>=4.0
ipykernel>=6.29
